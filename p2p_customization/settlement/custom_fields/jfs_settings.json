{
    "JFS Settings": [
        {
            "fieldname": "vendor_portal_tab",
            "fieldtype": "Tab Break",
            "label": "Vendor Portal",
            "insert_after": "impossible_travel_action",
            "module": "settlement"
        },
        {
            "fieldname": "enable_vendor_portal",
            "fieldtype": "Check",
            "label": "Enable Vendor Portal",
            "insert_after": "vendor_portal_tab",
            "default": "1",
            "description": "Master switch for the vendor portal -- vendor login and every page under /vendor-portal (dashboard, lists, details, profile, embedded edit views). Does NOT affect the vendor onboarding form itself, which stays reachable independently. Turn off to take the portal offline without changing any other configuration.",
            "module": "settlement"
        }
    ]
}
